NODE_ENV=development
PORT=4000
DATABASE_URL="file:./dev.db"

# REQUIRED: Must be explicitly set. Defaults to deny-all in production.
# Should be the frontend URL, e.g., http://localhost:3000 (dev) or https://app.meritpay.com (prod)
CORS_ORIGIN=http://localhost:3000
